#include <stdio.h>

int     main()
{
        int a = 5;
        if(a > 4)//if you have 1 line to execute, you do not need braces between code that executing
            printf("A is bigger than 4");
        return (0);
}